kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: estuary-viewer
  name: estuary-viewer-service
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    k8s-app: estuary-viewer
