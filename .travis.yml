language: node_js
node_js:
  - 7

services:
  - docker

env:

before_install:

install:

before_script:
  - npm install

script:
  - webdriver-manager update &
  - webdriver-manager start &
  - npm run serve &

after_success:


